#Day-26-Invert Binary Tree
<img width="1842" height="718" alt="image" src="https://github.com/user-attachments/assets/86359e44-2262-4776-a0fa-06e9d4e96713" />

#Code
class Solution {
public:
    TreeNode* invertTree(TreeNode* root) {
    if (!root) return nullptr;
    swap(root->left, root->right);
    invertTree(root->left); invertTree(root->right);
    return root;
}
